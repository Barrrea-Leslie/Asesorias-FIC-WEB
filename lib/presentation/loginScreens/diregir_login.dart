import 'package:flutter/material.dart';

class DiregirLogin extends StatelessWidget {
  const DiregirLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('LOGIN'),);
  }
}