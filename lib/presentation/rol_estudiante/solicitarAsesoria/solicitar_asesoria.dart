import 'package:flutter/material.dart';

class SolicitarAsesoria extends StatelessWidget {
  const SolicitarAsesoria({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Solicitar Asesorias'),
    );
  }
}