import 'package:flutter/material.dart';

class MaterialAdicional extends StatelessWidget {
  const MaterialAdicional({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Material Adicional"));
  }
}