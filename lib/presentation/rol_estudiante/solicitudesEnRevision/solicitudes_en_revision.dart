import 'package:flutter/material.dart';

class SolicitudesEnRevision extends StatelessWidget {
  const SolicitudesEnRevision({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Solicitudes en revision'),
    );
  }
}