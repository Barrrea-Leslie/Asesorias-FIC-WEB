import 'package:flutter/material.dart';

class Catalogos extends StatelessWidget {
  const Catalogos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("Offroad"));
  }
}
